# Database Design and Tools

- **Normalize data appropriately** – reduces redundancy and improves integrity.
- **Use stored procedures** – centralize frequently used logic, benefit from pre‑compilation and reduced network latency.
- **Monitor performance** – leverage tools like Query Store, execution plans, and index usage statistics.
- **Prevent SQL injection** – always use parameterized queries or stored procedures; never concatenate raw strings.
